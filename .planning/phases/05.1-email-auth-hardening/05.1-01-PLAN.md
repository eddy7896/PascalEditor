---
phase: 05.1-email-auth-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/editor/package.json
  - apps/editor/lib/email.ts
  - apps/editor/emails/PasswordResetEmail.tsx
  - apps/editor/emails/TeamInviteEmail.tsx
  - apps/editor/app/api/auth/forgot-password/route.ts
  - apps/editor/app/api/teams/invite/route.ts
autonomous: true
user_setup:
  - service: resend
    why: "Transactional email delivery for password reset and team invite"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
      - name: RESEND_FROM_EMAIL
        source: "Set to a Resend-verified sender address, e.g. noreply@yourdomain.com. For dev/test without a verified domain, leave unset — defaults to onboarding@resend.dev which only delivers to the Resend account owner email."

must_haves:
  truths:
    - "User who requests a password reset receives an email containing a reset link — the resetUrl is NOT returned in the API response body"
    - "OWNER/ADMIN who invites a team member triggers an email to the invitee — the inviteUrl is NOT returned in the API response body"
    - "forgot-password route stores SHA-256(token) in DB and puts the raw token in the email link"
    - "API responses for both endpoints return { success: true } with no URL field"
  artifacts:
    - path: "apps/editor/lib/email.ts"
      provides: "Resend singleton + typed sendEmail wrapper"
      exports: ["sendEmail"]
    - path: "apps/editor/emails/PasswordResetEmail.tsx"
      provides: "React Email template for password reset"
      exports: ["PasswordResetEmail"]
    - path: "apps/editor/emails/TeamInviteEmail.tsx"
      provides: "React Email template for team invite"
      exports: ["TeamInviteEmail"]
    - path: "apps/editor/app/api/auth/forgot-password/route.ts"
      provides: "Password reset endpoint that sends real email"
    - path: "apps/editor/app/api/teams/invite/route.ts"
      provides: "Team invite endpoint that sends real email"
  key_links:
    - from: "apps/editor/app/api/auth/forgot-password/route.ts"
      to: "apps/editor/lib/email.ts"
      via: "sendEmail() called with PasswordResetEmail template"
      pattern: "sendEmail"
    - from: "apps/editor/app/api/teams/invite/route.ts"
      to: "apps/editor/lib/email.ts"
      via: "sendEmail() called with TeamInviteEmail template"
      pattern: "sendEmail"
    - from: "apps/editor/app/api/auth/forgot-password/route.ts"
      to: "prisma.passwordResetToken"
      via: "stores SHA-256 hash, not raw token"
      pattern: "createHash.*sha256"
---

<objective>
Install Resend, create React Email templates for password reset and team invite, update both route handlers to send real email instead of returning URLs in the API response. Upgrade forgot-password to store SHA-256(token) in DB.

Purpose: Removes v1 dev shortcuts from production paths. After this plan, no auth endpoint leaks reset or invite URLs in HTTP responses.
Output: lib/email.ts singleton, two email templates, two updated route handlers.
</objective>

<execution_context>
@C:/Users/faheem/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/faheem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/editor/lib/email.ts
@apps/editor/lib/redis.ts
@apps/editor/app/api/auth/forgot-password/route.ts
@apps/editor/app/api/teams/invite/route.ts
@apps/editor/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Resend, create lib/email.ts singleton, create React Email templates</name>
  <files>
    apps/editor/package.json
    apps/editor/lib/email.ts
    apps/editor/emails/PasswordResetEmail.tsx
    apps/editor/emails/TeamInviteEmail.tsx
  </files>
  <action>
    1. Install dependencies in apps/editor/:
       `cd apps/editor && npm install resend @react-email/components`

    2. Create apps/editor/lib/email.ts:
       - Guard: `if (!process.env.RESEND_API_KEY) throw new Error('RESEND_API_KEY is not set')` at module level (fail fast)
       - Create singleton: `const resend = new Resend(process.env.RESEND_API_KEY)`
       - Export `FROM_EMAIL = process.env.RESEND_FROM_EMAIL ?? 'onboarding@resend.dev'`
       - Export async `sendEmail(opts: { to: string; subject: string; react: React.ReactElement }): Promise<void>`
         - Calls `resend.emails.send({ from: FROM_EMAIL, to: opts.to, subject: opts.subject, react: opts.react })`
         - If error, throws `new Error(\`Email send failed: \${error.message}\`)`

    3. Create apps/editor/emails/PasswordResetEmail.tsx:
       - Props: `{ resetUrl: string; name?: string }`
       - Use `Html, Head, Body, Container, Text, Link, Preview` from `@react-email/components`
       - Preview: "Reset your Pascal password"
       - Body: greeting using name if provided, instruction text ("expires in 1 hour"), `<Link href={resetUrl}>Reset password</Link>`, disclaimer ("If you did not request this, ignore this email.")
       - Named export: `PasswordResetEmail`

    4. Create apps/editor/emails/TeamInviteEmail.tsx:
       - Props: `{ inviteUrl: string; teamName: string; inviterName?: string; role: string }`
       - Same React Email imports
       - Preview: "You've been invited to join a Pascal team"
       - Body: inviter name (or "Someone"), team name, role, `<Link href={inviteUrl}>Accept invitation</Link>`, expiry note ("expires in 48 hours")
       - Named export: `TeamInviteEmail`
  </action>
  <verify>
    Run: `cd apps/editor && npx tsc --noEmit 2>&1 | head -30`
    Expect: no type errors in the new files.
    Also confirm resend appears in package.json dependencies.
  </verify>
  <done>
    lib/email.ts exports sendEmail, RESEND_API_KEY guard present.
    Both email templates compile without TypeScript errors.
    `resend` and `@react-email/components` in apps/editor/package.json.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update forgot-password and team invite routes to send real email (no URL in response)</name>
  <files>
    apps/editor/app/api/auth/forgot-password/route.ts
    apps/editor/app/api/teams/invite/route.ts
  </files>
  <action>
    ### forgot-password/route.ts

    Replace the existing file. Key changes from v1:

    - Add imports: `createHash` from `node:crypto`, `sendEmail` from `@/lib/email`, `PasswordResetEmail` from `@/emails/PasswordResetEmail`
    - Token generation: replace `randomBytes(32).toString('hex')` with:
      ```ts
      const raw = randomBytes(32).toString('hex')
      const hashed = createHash('sha256').update(raw).digest('hex')
      ```
    - Store `hashed` (not `raw`) in `prisma.passwordResetToken.create`: `data: { token: hashed, email: emailLower, expiresAt }`
    - Build resetUrl using `raw` (not hashed): `${base}/reset-password?token=${raw}`
    - Remove the `console.log` reset URL line entirely
    - Send email (fire-and-forget to preserve enumeration safety):
      ```ts
      if (user) {
        sendEmail({
          to: emailLower,
          subject: 'Reset your Pascal password',
          react: <PasswordResetEmail resetUrl={resetUrl} name={user.name ?? undefined} />,
        }).catch(err => console.error('[forgot-password] email send failed:', err))
      }
      ```
    - Final response: `return NextResponse.json({ success: true })` — NO `resetUrl` field

    NOTE: reset-password/route.ts (token redemption) must also be updated to hash the incoming token before lookup. Check if it does a raw lookup: `prisma.passwordResetToken.findUnique({ where: { token } })`. If so, update to: `const hashed = createHash('sha256').update(token).digest('hex'); prisma.passwordResetToken.findUnique({ where: { token: hashed } })`. Update that file in this same task.

    ### teams/invite/route.ts

    Additions only (keep all existing auth/validation logic):
    - Add imports: `sendEmail` from `@/lib/email`, `TeamInviteEmail` from `@/emails/TeamInviteEmail`
    - After `prisma.teamInviteToken.create(...)`, fetch the team name:
      ```ts
      const team = await prisma.team.findUnique({ where: { id: teamId }, select: { name: true } })
      ```
    - Fetch actor name for the invite email:
      ```ts
      const actor = await prisma.user.findUnique({ where: { id: actorId }, select: { name: true } })
      ```
    - Send email (fire-and-forget):
      ```ts
      sendEmail({
        to: email.toLowerCase(),
        subject: `You've been invited to join ${team?.name ?? 'a Pascal team'}`,
        react: <TeamInviteEmail
          inviteUrl={inviteUrl}
          teamName={team?.name ?? 'a team'}
          inviterName={actor?.name ?? undefined}
          role={role}
        />,
      }).catch(err => console.error('[team-invite] email send failed:', err))
      ```
    - Change final response from `{ success: true, inviteUrl, expiresAt }` to `{ success: true, expiresAt }` — remove `inviteUrl` from response body

    File to also update if needed: apps/editor/app/api/auth/reset-password/route.ts (SHA-256 lookup upgrade).
  </action>
  <verify>
    1. `cd apps/editor && npx tsc --noEmit 2>&1 | head -30` — no errors
    2. Search for `resetUrl` in the response of forgot-password/route.ts: should NOT appear in any `NextResponse.json(` call
    3. Search for `inviteUrl` in the response of teams/invite/route.ts: should NOT appear in any `NextResponse.json(` call
    4. Confirm `createHash('sha256')` is present in forgot-password/route.ts
  </verify>
  <done>
    forgot-password POST returns { success: true } only — no resetUrl field.
    teams/invite POST returns { success: true, expiresAt } — no inviteUrl field.
    PasswordResetToken stores SHA-256 hash; raw token goes in email link.
    reset-password redemption hashes incoming token before DB lookup.
    TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in apps/editor/
- grep for `resetUrl` in forgot-password/route.ts response body returns no match
- grep for `inviteUrl` in teams/invite/route.ts response body returns no match
- lib/email.ts present with RESEND_API_KEY guard
- Both React Email templates present under apps/editor/emails/
</verification>

<success_criteria>
Password reset and team invite endpoints send real email via Resend and return no URL in the response body. SHA-256 token hashing in place for password reset. Code compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-email-auth-hardening/05.1-01-SUMMARY.md`
</output>
