---
phase: 05.4-audit-notifications
plan: 03
type: execute
wave: 2
depends_on: ["05.4-01"]
files_modified:
  - apps/editor/app/api/notifications/route.ts
  - apps/editor/app/dashboard/_components/NotificationBell.tsx
  - apps/editor/app/dashboard/layout.tsx
autonomous: true

must_haves:
  truths:
    - "GET /api/notifications returns { unreadCount, items[] } for the current session user"
    - "PATCH /api/notifications marks all unread notifications as read for the current user"
    - "NotificationBell client component renders a bell icon with an unread badge when unreadCount > 0"
    - "Clicking the bell opens a dropdown listing recent notifications with relative timestamps and a 'mark all read' action"
    - "Bell polls every 30s and refetches on window focus to keep unread count fresh"
    - "Bell is mounted in the dashboard layout (sidebar/header) so it's visible across all dashboard pages"
  artifacts:
    - path: "apps/editor/app/api/notifications/route.ts"
      provides: "GET (list + unread count) and PATCH (mark all read) endpoints"
      exports: ["GET", "PATCH"]
    - path: "apps/editor/app/dashboard/_components/NotificationBell.tsx"
      provides: "Client bell component with dropdown"
      contains: "use client"
      min_lines: 60
    - path: "apps/editor/app/dashboard/layout.tsx"
      provides: "Mounts NotificationBell in dashboard chrome"
      contains: "NotificationBell"
  key_links:
    - from: "apps/editor/app/dashboard/_components/NotificationBell.tsx"
      to: "/api/notifications"
      via: "fetch in useEffect with polling interval"
      pattern: "fetch\\([\"'`]/api/notifications"
    - from: "apps/editor/app/api/notifications/route.ts"
      to: "lib/notifications.ts"
      via: "getUnreadNotificationCount, getRecentNotifications, markAllNotificationsRead"
      pattern: "getUnreadNotificationCount|getRecentNotifications|markAllNotificationsRead"
---

<objective>
Build the in-app notification bell: an API route, a polling client component, and integration into the dashboard chrome.

Purpose: Satisfies success criterion 2 (in-app notification bell shows unread count and dropdown of recent notifications).
Output: Read API + mark-read API + bell UI mounted in dashboard layout.
</objective>

<execution_context>
@C:/Users/faheem/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/faheem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.4-audit-notifications/05.4-RESEARCH.md
@.planning/phases/05.4-audit-notifications/05.4-01-SUMMARY.md
@apps/editor/lib/notifications.ts
@apps/editor/app/dashboard/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/notifications route handler (GET + PATCH)</name>
  <files>apps/editor/app/api/notifications/route.ts</files>
  <action>
  Create a Next.js App Router route handler at `apps/editor/app/api/notifications/route.ts`.

  GET:
  - Resolve session via `getServerSession(authOptions)`. If no userId, return `NextResponse.json({ unreadCount: 0, items: [] }, { status: 401 })`.
  - Call `getUnreadNotificationCount(userId)` and `getRecentNotifications(userId, 20)` in parallel via `Promise.all`.
  - Return `NextResponse.json({ unreadCount, items })`.
  - Items should serialize cleanly (Date → ISO string is automatic via JSON).

  PATCH:
  - Resolve session. If no userId, return 401.
  - Call `markAllNotificationsRead(userId)`.
  - Return `NextResponse.json({ ok: true })`.

  Use existing imports from `@/lib/auth` for authOptions (match other API routes in the project). Add `export const dynamic = "force-dynamic"` to disable caching.
  </action>
  <verify>
  - `curl -X GET http://localhost:3000/api/notifications` (with auth cookie) returns `{ unreadCount, items }`
  - `curl -X PATCH http://localhost:3000/api/notifications` returns `{ ok: true }` and subsequent GET shows unreadCount = 0
  - `pnpm --filter editor tsc --noEmit` passes
  </verify>
  <done>Both verbs work with proper auth gating and use the lib/notifications helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Build NotificationBell component and mount it in dashboard layout</name>
  <files>
    apps/editor/app/dashboard/_components/NotificationBell.tsx
    apps/editor/app/dashboard/layout.tsx
  </files>
  <action>
  Create `apps/editor/app/dashboard/_components/NotificationBell.tsx` as a client component (`"use client"`).

  State:
  - `unreadCount: number` (init 0)
  - `items: Notification[]` (init [])
  - `open: boolean` (dropdown visibility)
  - `loading: boolean`

  Behavior:
  - On mount, fetch GET /api/notifications and populate state.
  - Set up polling: `setInterval` every 30000ms calling the same fetch. Clear on unmount.
  - Add a `window` focus listener that triggers an immediate refetch when the tab regains focus.
  - Bell icon: import `Bell` from `lucide-react`. Render a button with the icon. If `unreadCount > 0`, render a small red badge with the number (cap display at "9+" if > 9).
  - Click bell → toggle `open`. When opening, fire PATCH /api/notifications to mark all read, then optimistically zero `unreadCount` (keep `items` so user can still see them in the dropdown).
  - Dropdown panel (absolute-positioned div below the bell):
    - Heading "Notifications"
    - List of `items` with: type-formatted message, relative timestamp (use `Intl.RelativeTimeFormat` or existing date-fns if present in package.json), unread style (subtle bg) when `readAt == null` at fetch time.
    - Format messages by `type`:
      - `SCENE_DUPLICATED`: "Your scene {meta.assetTitle} was duplicated"
      - `ADDED_TO_TEAM`: "You were added to {meta.teamName}"
      - `ROLE_CHANGED`: "Your role in {meta.teamName} was changed to {meta.newRole}"
    - Empty state: "No notifications yet."
    - Click outside to close (use a ref + document mousedown listener).
  - Style with Tailwind matching existing dashboard chrome.

  Mount in `apps/editor/app/dashboard/layout.tsx`:
  - Import NotificationBell.
  - Place it in the existing top-bar/header area next to the user avatar/menu (read the current layout first to find the right slot — match alignment of existing controls).
  - Do NOT block SSR: render the bell as a client component; it self-fetches.
  </action>
  <verify>
  - Visit /dashboard → bell visible in header
  - Insert a Notification row directly via Prisma Studio with `readAt: null` → bell shows badge "1" within 30s (or sooner on focus)
  - Click bell → dropdown opens, shows the item, badge clears
  - `pnpm --filter editor tsc --noEmit` passes
  - `pnpm --filter editor lint` passes
  </verify>
  <done>Bell is visible across all dashboard pages, polls + refreshes on focus, badge accurately reflects unread count, dropdown lists recent items with correct messages, click marks all as read.</done>
</task>

</tasks>

<verification>
- API endpoints respond correctly with auth gating
- Bell renders in dashboard, polling works, dropdown displays items
- Mark-as-read updates DB and UI
</verification>

<success_criteria>
- Phase success criterion 2 satisfied: in-app notification bell shows unread count and a dropdown of recent notifications
</success_criteria>

<output>
After completion, create `.planning/phases/05.4-audit-notifications/05.4-03-SUMMARY.md`
</output>
