---
phase: 05.4-audit-notifications
plan: 03
subsystem: api, ui
tags: [notifications, nextjs, polling, lucide-react, tailwind]

# Dependency graph
requires:
  - phase: 05.4-audit-notifications
    plan: 01
    provides: getUnreadNotificationCount, getRecentNotifications, markAllNotificationsRead helpers in lib/notifications.ts
provides:
  - GET /api/notifications — returns { unreadCount, items[] } for current session user
  - PATCH /api/notifications — marks all unread notifications as read
  - NotificationBell client component with polling + dropdown
  - Bell mounted in dashboard layout header (visible on all dashboard pages)
affects:
  - Any future notification-producing features (badge stays fresh via poll + focus refetch)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Polling with setInterval + window focus listener for fresh unread count
    - Optimistic UI: zero badge immediately on click, fire-and-forget PATCH in background
    - Click-outside pattern via useRef + document mousedown listener

key-files:
  created:
    - apps/editor/app/api/notifications/route.ts
    - apps/editor/app/dashboard/_components/NotificationBell.tsx
  modified:
    - apps/editor/app/dashboard/layout.tsx

key-decisions:
  - "Notification bell placed in sticky header above BreadcrumbNav — no top-bar existed; added minimal header row with right-aligned bell"
  - "Relative timestamps via Intl.RelativeTimeFormat (no date-fns in project)"
  - "Optimistic unread-zero on bell open — UX snappiness; reconciles on next 30s poll if PATCH fails"

# Metrics
metrics:
  duration: ~15 min
  completed: 2026-05-03
  tasks_completed: 2
  tasks_total: 2
  files_created: 2
  files_modified: 1
---

# Phase 05.4 Plan 03: Notification Bell UI Summary

**In-app notification bell with polling API route (GET/PATCH) and dropdown listing recent notifications, mounted in the dashboard header.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-03T00:01:31Z
- **Completed:** 2026-05-03
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- `/api/notifications` route handler (GET returns unreadCount + items array; PATCH marks all read)
- NotificationBell component: 30s poll, window focus refetch, red badge (9+ cap), click-outside close
- Bell mounted in sticky dashboard header — visible across all dashboard pages

## Task Commits

1. **Task 1: Create /api/notifications route handler** - `d771b41` (feat)
2. **Task 2: Build NotificationBell and mount in dashboard layout** - `95b69cb` (feat)

## Files Created/Modified

- `apps/editor/app/api/notifications/route.ts` — GET + PATCH endpoints, force-dynamic, auth gated
- `apps/editor/app/dashboard/_components/NotificationBell.tsx` — Client bell component with polling + dropdown
- `apps/editor/app/dashboard/layout.tsx` — Added NotificationBell import + sticky header slot

## Decisions Made

- Notification bell placed in new sticky `<header>` row above main content (no pre-existing top-bar to slot into)
- Relative timestamps use `Intl.RelativeTimeFormat` — no date-fns in project
- Optimistic badge zero on click: UX snappiness, reconciles on next poll if PATCH fails

## Deviations from Plan

None — plan executed exactly as written. The sticky header was added as the mounting slot (plan said "find the right slot — match alignment of existing controls"); this matches the dashboard chrome style without adding scope.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Phase success criterion 2 satisfied: bell shows unread count + recent notifications dropdown
- Notification callsite writes (05.4-02 plan) can now be verified end-to-end via the bell UI

---
*Phase: 05.4-audit-notifications*
*Completed: 2026-05-03*
