---
phase: 05.3-search-navigation-settings
plan: 02
type: execute
wave: 2
depends_on: ["05.3-01"]
files_modified:
  - apps/editor/app/dashboard/_components/SortFilterControls.tsx
  - apps/editor/app/dashboard/projects/page.tsx
  - apps/editor/app/dashboard/teams/[teamId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can change sort order between Name, Last Modified, Last Opened — order updates immediately"
    - "User can filter projects by All, Recent, Starred, Archived — list updates accordingly"
    - "Selected sort/filter persists in URL as ?sort=... &filter=... and survives refresh"
    - "On a team page (/dashboard/teams/[teamId]), sort/filter scope to that team only (teamId passed to searchProjects)"
  artifacts:
    - path: "apps/editor/app/dashboard/_components/SortFilterControls.tsx"
      provides: "Sort and filter dropdown buttons that update URL params"
      min_lines: 60
  key_links:
    - from: "apps/editor/app/dashboard/_components/SortFilterControls.tsx"
      to: "URL searchParams (?sort= ?filter=)"
      via: "router.replace + URLSearchParams"
      pattern: "params\\.set\\(.sort."
    - from: "apps/editor/app/dashboard/projects/page.tsx"
      to: "searchProjects with sort + filter"
      via: "searchParams.sort, searchParams.filter passthrough"
      pattern: "sort:.*sp\\.sort"
    - from: "apps/editor/app/dashboard/teams/[teamId]/page.tsx"
      to: "searchProjects scoped by teamId"
      via: "teamId param from route"
      pattern: "teamId: params\\.teamId"
---

<objective>
Add sort and filter controls to the dashboard project grid; both global (/dashboard/projects) and team-scoped (/dashboard/teams/[teamId]) views support them.

Purpose: Users navigate large project sets quickly; sort by name / last modified / last opened, filter by all / recent / starred / archived. Selections live in URL for bookmarkability.
Output: SortFilterControls client component; projects page and team page consume sort/filter searchParams.
</objective>

<execution_context>
@C:/Users/faheem/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/faheem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05.3-search-navigation-settings/05.3-RESEARCH.md
@.planning/phases/05.3-search-navigation-settings/05.3-01-SUMMARY.md
@apps/editor/app/dashboard/actions.ts
@apps/editor/app/dashboard/projects/page.tsx
@apps/editor/app/dashboard/teams/[teamId]/page.tsx
@apps/editor/app/dashboard/_components/SearchInput.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SortFilterControls client component</name>
  <files>apps/editor/app/dashboard/_components/SortFilterControls.tsx</files>
  <action>
    Create a `'use client'` component `SortFilterControls` that renders two dropdown buttons (Sort, Filter) styled to match dashboard (zinc-800/60 bg, zinc-700 border, lucide ChevronDown icon). Reference research file lines 326-401 for the exact pattern.

    Props:
    ```ts
    type SortOption = 'name' | 'modified' | 'opened';
    type FilterOption = 'all' | 'recent' | 'starred' | 'archived';
    export function SortFilterControls({ currentSort, currentFilter }: { currentSort: SortOption; currentFilter: FilterOption })
    ```

    Behavior:
    - Use `useRouter`, `useSearchParams`, `usePathname` from 'next/navigation'.
    - Local `useState` booleans for which dropdown is open (only one open at a time — closing one when other opens is OK, not required).
    - On option click: build `new URLSearchParams(searchParams)`, `params.set('sort', value)` (or 'filter'), call `router.replace(\`${pathname}?${params.toString()}\`)`. This preserves any existing `q`, `teamId`, etc. (avoids Pitfall 5 in research).
    - Sort options labeled: Name, Last Modified, Last Opened (mapping: name, modified, opened).
    - Filter options labeled: All, Recent, Starred, Archived (mapping: all, recent, starred, archived).
    - Display current selection in button label (e.g., "Sort: Name").
    - Click outside to close: implement with a simple `useEffect` adding `mousedown` listener that checks `event.target` against a ref; OR keep both open until re-clicked. Choose simpler: close-on-select only.
    - Add `data-testid="sort-control"` and `data-testid="filter-control"` to outer wrappers for easier verification.

    Export named `SortFilterControls`.
  </action>
  <verify>
    `cd apps/editor && bunx tsc --noEmit` passes. Component file ≥60 lines including imports/props/markup.
  </verify>
  <done>SortFilterControls component compiles; renders two URL-driven dropdowns with correct option mappings.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SortFilterControls into projects page and team page</name>
  <files>apps/editor/app/dashboard/projects/page.tsx, apps/editor/app/dashboard/teams/[teamId]/page.tsx</files>
  <action>
    1. apps/editor/app/dashboard/projects/page.tsx (already updated in 05.3-01):
       - Read existing file. Confirm it already calls `searchProjects({ query, sort, filter, teamId })`.
       - Add import for `SortFilterControls`.
       - Render `<SortFilterControls currentSort={(sp.sort as any) ?? 'name'} currentFilter={(sp.filter as any) ?? 'all'} />` directly below `<SearchInput />`, above the project grid.
       - Ensure `sp.sort` and `sp.filter` are passed into searchProjects call (already wired per 05.3-01 — verify, do not duplicate).

    2. apps/editor/app/dashboard/teams/[teamId]/page.tsx:
       - Read existing file first. It currently fetches team-scoped projects (via getDashboardData or a team query).
       - Convert to async server component if not already, accepting `params: Promise<{ teamId: string }>` AND `searchParams: Promise<{ q?: string; sort?: string; filter?: string }>` (Next.js 16 patterns).
       - Replace the team-scoped project fetch with: `const projects = await searchProjects({ query: sp.q, sort: sp.sort as any, filter: sp.filter as any, teamId: p.teamId });`
       - PRESERVE existing team-data fetching (team name, members, role) needed for the page header — only replace the projects array source.
       - Add `<SearchInput placeholder={\`Search ${team.name} projects…\`} />` and `<SortFilterControls currentSort={...} currentFilter={...} />` above the projects grid.
       - Ensure team membership is verified before render (existing requireTeamRole or assertTeamMember guard preserved).

    3. Do NOT change RBAC guards, ProjectCard, or DashboardSidebar.
  </action>
  <verify>
    `cd apps/editor && bunx tsc --noEmit` passes. Manual: at /dashboard/projects?q=test, click sort dropdown → choose "Last Modified" → URL becomes `?q=test&sort=modified`, list re-orders by updatedAt desc. Choose filter "Archived" → URL gains `&filter=archived`, list now shows soft-deleted projects only. Visit /dashboard/teams/{anyTeamId} → search/sort/filter scoped to that team's projects only.
  </verify>
  <done>Both pages render SortFilterControls; URL params drive ordering and filtering; team page scopes to teamId; existing team metadata still rendered.</done>
</task>

</tasks>

<verification>
- TypeScript clean across both files.
- All four searchParams (q, sort, filter, teamId) preserved across each other when changed individually (Pitfall 5).
- Archived filter is the ONLY way to see soft-deleted projects (Pitfall 3).
</verification>

<success_criteria>
- Sort and filter dropdowns visible on /dashboard/projects and /dashboard/teams/[teamId].
- URL is the source of truth for sort/filter state.
- Team page scopes results to that team's teamId.
- No regression in existing team page header / member display.
</success_criteria>

<output>
Create `.planning/phases/05.3-search-navigation-settings/05.3-02-SUMMARY.md` documenting: SortFilterControls API, sort/filter option mappings, how teamId scoping flows through, any UI choices (close-on-select behavior, etc.).
</output>
