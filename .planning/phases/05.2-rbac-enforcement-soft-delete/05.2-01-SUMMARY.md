---
phase: 05.2-rbac-enforcement-soft-delete
plan: "01"
subsystem: auth
tags: [rbac, prisma, typescript, teamspaces, permissions]

requires:
  - phase: 05.1-email-auth-hardening
    provides: Auth session with userId available in server actions

provides:
  - lib/rbac-guards.ts with PermissionError, requireTeamRole, requireProjectRole, assertTeamMember
  - Role hierarchy constants (team: 5-level, project: 4-level)

affects:
  - 05.2-rbac-enforcement-soft-delete (plans 02+)
  - dashboard/actions.ts (renameProject, deleteProject, createProject)

tech-stack:
  added: []
  patterns:
    - "PermissionError subclass as the single 403-equivalent error for all RBAC failures"
    - "Numeric rank maps for role hierarchy comparison (teamRoleHierarchy, projectRoleHierarchy)"

key-files:
  created:
    - apps/editor/lib/rbac-guards.ts
  modified: []

key-decisions:
  - "requireTeamRole used for mutations (rename, delete); assertTeamMember for createProject (any role gate)"
  - "Role ranks stored as Record<TeamRole|ProjectRole, number> — easy to extend when new roles added"
  - "PermissionError extends Error with Object.setPrototypeOf to restore prototype chain in TS/ES5 targets"

patterns-established:
  - "Role hierarchy: numeric rank comparison; throw PermissionError if actualRank < requiredRank"
  - "All RBAC functions import from @/lib/rbac-guards — single source of truth"

duration: 10min
completed: 2026-04-30
---

# Phase 05.2 Plan 01: RBAC Guards Library Summary

**Shared `lib/rbac-guards.ts` with typed role hierarchy validators and PermissionError for team/project permission enforcement**

## Performance

- **Duration:** 10 min
- **Started:** 2026-04-30T00:00:00Z
- **Completed:** 2026-04-30T00:10:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- `PermissionError` custom Error subclass with `name='PermissionError'` and prototype chain fix
- `requireTeamRole` with 5-level OWNER>ADMIN>EDITOR>COMMENTER>VIEWER hierarchy
- `requireProjectRole` with 4-level OWNER>EDITOR>COMMENTER>VIEWER hierarchy
- `assertTeamMember` lightweight membership-only gate (no role minimum)
- All functions use named `findUnique` queries with `teamId_userId` compound unique indexes

## Task Commits

1. **Task 1: Create rbac-guards.ts with role hierarchy validators** - `ef20ec4` (feat)

**Plan metadata:** (docs commit to follow)

## Files Created/Modified

- `apps/editor/lib/rbac-guards.ts` — Exports PermissionError class and 3 guard functions with JSDoc, 152 lines

## Decisions Made

- `requireTeamRole` used for mutations (rename, delete); `assertTeamMember` for createProject (any role gate, not role-specific)
- Role ranks stored as `Record<TeamRole|ProjectRole, number>` — easy to extend when new roles are added to schema
- `PermissionError` extends Error with `Object.setPrototypeOf` to restore prototype chain in TS/ES5 compilation targets

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `lib/rbac-guards.ts` is ready to be imported into `dashboard/actions.ts` for plan 05.2-02
- `renameProject`, `deleteProject` can now call `requireTeamRole(teamId, userId, "EDITOR")` to enforce 403 for VIEWERs
- `createProject` can call `assertTeamMember(teamId, userId)` before creating the project record

---
*Phase: 05.2-rbac-enforcement-soft-delete*
*Completed: 2026-04-30*
